{
  "name": "sui-note",
  "private": true,
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --workspaces build",
    "test": "bun test ./tests",
    "lint": "bun run --workspaces lint"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2"
  }
}
