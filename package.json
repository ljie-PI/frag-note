{
  "name": "frag-note",
  "private": true,
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun --env-file=.env run --workspaces build",
    "test": "bun --env-file=.env test ./tests --ignore=\"tests/e2e/**\"",
    "test:e2e": "bun --env-file=.env test tests/e2e/ --timeout 30000 --ignore=\"tests/e2e/full/**\"",
    "test:e2e:full": "bun --env-file=.env test tests/e2e/full/ --timeout 120000",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "@types/bun": "^1.3.2"
  }
}
