{
  "name": "frag-note",
  "private": true,
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun --env-file=.env run --workspaces build",
    "test": "bun --env-file=.env test ./tests",
    "lint": "bun --env-file=.env run --workspaces lint"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2"
  }
}
